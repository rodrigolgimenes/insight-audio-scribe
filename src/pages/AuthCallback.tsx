
import { AuthCallback as AuthCallbackComponent } from "@/components/auth/AuthCallback";

// Re-export the AuthCallback component from auth directory
export default function AuthCallback() {
  return <AuthCallbackComponent />;
}
