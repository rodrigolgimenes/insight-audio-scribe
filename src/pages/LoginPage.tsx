
import { LoginPage as AuthLoginPage } from "@/components/auth/LoginPage";

// Re-export the LoginPage component from auth directory
export default function LoginPage() {
  return <AuthLoginPage />;
}
